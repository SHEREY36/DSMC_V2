[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "coll-models-v2"
version = "2.1.0"
requires-python = ">=3.10"
dependencies = ["numpy>=1.23", "scipy>=1.9", "dsmc-v2-contracts==2.1.0"]

[tool.setuptools.packages.find]
where = ["src"]
